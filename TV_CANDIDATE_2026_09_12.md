# TV implementation and joint testing checkpoint — 2026-09-12

Android TV now has a native Compose application in `viptv-org/android` (candidate `7b666f9`, execution issue #3). Tizen and Vizio share the React application in `viptv-org/tv-web` (spec #1, execution #2). Their UI source is shared; playback is AVPlay on Tizen and HTML media with backend delivery on Vizio. The backend candidate `af35a6a` adds a separate `/tv` static mount while preserving the account dashboard and APIs. The backend candidate has not been deployed to production. Android candidate `ee96676` is installed on the authorized physical Android TV device; `d9b78fe` awaits hosted build and retest below.

The owner resumed testing and requested Android/web design and architecture conformance. Emulators remain deferred after server OOM. Use hosted Android builds; no local Gradle/emulator. A single browser worker with a 256 MB Node heap was used after checking available memory, with agents coordinating execution. Historical passes do not certify later source changes. Both app repositories record specific acceptance checklists and hosted-runner workflows.

Implemented shared TV behaviors include release-only OK with 700 ms secondary hold; focus/history restoration; pairing, profile/avatar editing and parent PIN; source-labelled Search; manual Sources and exact saved-source Resume; queue corrections/hide/undo; guide two-hour window/current/future actions; native versus managed seek/track handling; bounded controlled Next with cancellation; preferences and addon management. The existing specs remain the acceptance criteria, not a claim that every implementation detail already passes them.

During joint testing, compare every screen against specs/visual and exercise specs/behavior/roku-ux-contract.md. Prioritize source intent, final-ten-second eligibility, cancellation while a candidate opens, same-provider IPTV Next, previous-episode Resume, held OK release suppression, profile isolation, guide geometry and focus restoration. Record each difference as a linked ticket; update design only for an accepted behavior change. Retain screenshots only as ignored local inspection evidence.

Tizen packaging currently creates an unsigned hosted launcher candidate. Samsung signing, remote-hosted AVPlay availability and real TV behavior require verification. Vizio hosting must use the backend HTTPS origin; its model-specific HLS/codec and track behavior remains unverified. Transcoding remains last resort. Roku code and production data are unchanged.

## Resumed acceptance evidence

The latest shared TV web acceptance run [34714994356](https://github.com/viptv-org/tv-web/actions/runs/34714994356) passed at `a3e0de5`: 40 unit tests, 35 browser tests, 23 intentional platform exclusions, production build and platform packaging. These include real Chromium WebM decoding, HTTP request contracts, parent-gated profile/sign-out cancellation, queue hold, source identity mismatch, Discover filters, partial search failures, managed seek/subtitle requests and five controlled Next/Resume scenarios. See `tv-web/tests/PARITY_MATRIX.md` for states still requiring coverage. Physical Tizen/Vizio decoding and remote-hosted AVPlay remain unqualified.

Android hosted run [34714435017](https://github.com/viptv-org/android/actions/runs/34714435017) passed app unit tests and APK assembly at `ee96676`. Physical installation as an update retained pairing. Launch, real profile selection, populated Home artwork, detail/manual source selection, actual video decoding and Back stopping playback passed on the authorized Android 14 TV. Tests use a dedicated temporary QA profile to preserve existing viewing history; remove that owned profile after acceptance.

The same device pass exposed defects: no progress persisted on Back, media keys lost focus when the overlay disappeared, source rows lacked human metadata, detail/source initial focus was missing, and unfocused Compose borders rendered hairlines. Candidate `d9b78fe` addresses these and adds the canonical player overlay, track dialogs, persistent media-key focus, periodic/pre-stop progress snapshots, explicit playback return destinations, source metadata and root-library test gates. Its hosted build and physical retest are pending; it is not yet accepted. Development signing is now stable so updates preserve pairing. Keep machine-specific access only in the ignored private development file; never preserve temporary pairing codes.

Design revision `8344c91` corrects the extracted Resume identity rule to match frozen Roku: addon ID plus a nonempty stable source fingerprint. Display names and ephemeral stream IDs do not authorize automatic Resume. Missing or mismatched identity requires manual source choice. This is an extraction correction, not a Roku behavior change.
